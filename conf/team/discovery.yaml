name: "Discovery Team"
tasks:
  - name: "discovery_phase" # Unique instance name
    agent_type: "GenericLLMAgent" # Use LLM agent for testing
    persona_key: "discovery_v1" # Use simpler discovery persona
    description: "Analyze project structure and files" # Updated description
    requires_approval: false
    max_retries: 2
routing:
  default: "solution"